json.extract! product, :id, :name, :description, :image, :if_free, :original_price, :price, :savings, :percent_discount, :category, :created_at, :updated_at
json.url product_url(product, format: :json)